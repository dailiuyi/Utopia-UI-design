# 手机 MR 决斗 UI · 复用包

这份包将《攻壳机动队》的冷峻科技气质提炼为 Unity 手机 MR 界面约定：冷黑青灰、电子绿、青色信息线与精密机械结构。默认以 1995 剧场版气质为起点，不将项目色值声明为官方规范。它不是 Unity 成品工程或现成 UI 插件。

## 从这里开始

| 用途 | 文件 |
|---|---|
| 看界面方向，切换亮暗环境和候选操作方式 | [本地设计预览](docs/design-preview.html) |
| 给支持 Skill 的 Agent | [utopia-mr-ui/SKILL.md](skills/utopia-mr-ui/SKILL.md) 及整个所在目录 |
| 给普通聊天/开发 Agent | [可复制提示词](prompts/unity-ui-agent.md) |
| 查看完整变量 | [design-tokens.json](skills/utopia-mr-ui/assets/design-tokens.json) |
| 查看定位、旋转、操作模块的边界 | [界面与状态](skills/utopia-mr-ui/references/screens-and-states.md) |

目录整体可复制。Skill 内含两张原始黑黄海报，仅辅助构图与机械细节，当前主题以文字视觉规范与设计变量为准。必要规范不依赖本机绝对路径、联网工具或其他 Skill。其他 Agent 不能看图时，可使用完整文字视觉规范。暂未安装到任何全局技能目录；在目标 Agent 支持的技能目录中放入 `utopia-mr-ui` 文件夹，或直接让它读取本地 `SKILL.md` 及引用文件。不同工具的自动发现方式以其配置为准。

最短调用语句：

```text
请先读取 skills/utopia-mr-ui/SKILL.md 以及它要求的相关文件，按这份规范为当前 Unity 6 项目完成中文“准备页、重定位页、共享战斗 HUD”。先检查现有工程；本次先输出设计与接入方案，操作模块继续保留未定。
```

需要编码时，把最后一句改为具体获准实现的范围，例如“本次实现准备页，并接入已有的进入重定位事件”。如果工具已发现此 Skill，也可直接调用 `$utopia-mr-ui`。不要只复制入口文件而漏掉 references 与 assets。

## 需求记录

| 已确认 | 仍需后续确定 |
|---|---|
| Android + iOS 手机，Unity 6，中文 | Unity 具体小版本，既有 uGUI/UI Toolkit，目标工程 |
| 竖屏准备、横屏战斗 | 直接操作/指令/观战，按钮与招式规则 |
| 重定位现实场景后显示决斗场，类似拳皇对战呈现 | 定位 SDK、登记地图格式、真实状态/事件 |
| 《攻壳机动队》配色与科技风格；默认冷色方案 | 联网/本地对战、最低机型、性能目标、游戏与角色正式名称 |

配色数值、字级、动效和页面结构是可调整的设计建议，不是官方色表或逐帧取色结论。上述竖屏准备/横屏决斗为原始模板，复用时尊重现有工程的玩法、朝向与身份色。预览中的角色、场景、血量与计时均为示意。操作模式切换是预览工具，用来讨论候选布局，不代表游戏要加入模式选择功能。

## 产物与验证边界

预览只演示外观、布局和候选操作区；没有访问相机、调用定位、运行战斗或生成真实 Unity 场景。正式客户端仍需字体资源、角色/场地资源与已有业务事件接入，并分别做 Android 和 iPhone 真机验证。

包的结构、引用、预览与独立 Agent 试用结果见 [验证记录](docs/validation.md)。本次没有决定具体操作方式，也没有将其他硬件加入范围。
