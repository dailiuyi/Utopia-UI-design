# 验证记录

验证日期：2026-09-22。本记录针对更新后的攻壳科技风规范、可复用包和本地预览，不是 Unity 游戏验收。

| 检查 | 结果与边界 |
|---|---|
| 风格一致性 | 入口、视觉规范、设计变量、Agent 元数据、可复制提示词、HTML 统一为冷黑青灰、电子绿、青色信息线；黑黄海报仅为辅助参考 |
| Skill 结构 | 使用 Python UTF-8 模式执行 skill-creator 的 quick_validate.py，结构验证通过 |
| 数据与本地引用 | check-package.py 检查 JSON/YAML、Markdown 本地引用、参考海报副本与 ZIP 内容；当前脚本从自身路径确定工作区 |
| 独立 Agent 试用 | 只读使用新 skill 为竖屏红蓝方块单摇杆项目拟方案，保留实际玩法与朝向；发现的模板纵横屏/生命值歧义已加实际项目条件 |
| 浏览器执行 | Edge 无头浏览器加载新预览，无页面 JS 错误、无 HTTP/HTTPS 请求 |
| 预览交互 | 明暗环境及未定/直接操控/指令/观战四种候选模块检查通过，观战隐藏两侧操作区 |
| 布局检查 | 1400×1100 桌面、390×844 窄屏检查通过；窄屏文档无横向溢出 |
| 静态视觉检查 | 已查看更新后的完整设计板截图，确认冷色、无衬线标题、信息线与准备/定位/战斗布局；另目视检查新版红蓝方块四屏生成图 |
| Unity Editor | 未验证：本目录没有 Unity 工程，未编译或接入 C#、Prefab、场景 |
| Android / iPhone 真机 | 均未验证：相机、定位、真实玩法、输入、安全区、性能与物理触摸尺寸仍需实测 |
| 玩法确认 | 通用决斗预览仍展示操作候选；红蓝方块 demo 保留目标源码的单摇杆。两者均不代表可玩版本 |

截图：[完整设计板](preview-desktop.png)、[横屏暗环境](preview-battle.png)、[横屏亮环境](preview-battle-light.png)、[窄屏设计板](preview-mobile.png)。

浏览器预览采用原创几何环境示意，只能切换示意光照与候选操作模块；画面内产品按钮不绑定业务。没有访问相机、创建对局或部署网站。明暗背景不能代替阳光、暗室和复杂真实场景的可读性验证。视口检查也不等于手机触摸尺寸验收。

本地 view_image 因沙箱初始化失败无法打开；完整设计板通过只读图片字节加载后实际目视检查。首次结构验证因 Windows 默认 GBK 读取 UTF-8 失败，改用 Python -X utf8 重跑，不修改系统编码。
